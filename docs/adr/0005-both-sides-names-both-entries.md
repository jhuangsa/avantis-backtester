# A both-sides hypothesis names both entries

A both-sides hypothesis names its long entry and its short entry, and each side may name its own rule exit. The second side is not produced by flipping the first. A mirror is the right short for a moving-average cross and the wrong short for a threshold, so a hypothesis that wants the mirror writes the mirrored rule itself.
